#include <bits/stdc++.h>

using namespace std;


int main() {
    int T; cin >> T;
    int i = 1;
    while (T--) {
        int a, b; cin >> a >> b;
        cout << "Case "<< i<<": "<<a+b<<endl;
        i ++;
    }

    return 0;
}
